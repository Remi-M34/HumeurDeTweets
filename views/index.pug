doctype html
html(lang='fr')
  head
    title Accueil
    meta(charset='UTF8')
    link(rel='stylesheet', href='stylesheets/style.css')
    link(rel='shortcut icon', type='image/x-icon', href='images/favicon.ico')
    link(href='https://fonts.googleapis.com/css?family=Roboto+Condensed', rel='stylesheet')
    link(rel='stylesheet', type='text/css', href='http://fonts.googleapis.com/css?family=Lato')
  body
    header
      img.logo(src='/images/um.png', width='800')
      h2
        | Humeur
        h2

    .page
      .descriptionaccueil
        h2  ...
        h4  .
        hr
      .pageaccueil
        br
        .twitter
          | Ci-dessous le fil Twitter.
          br
          |  Suivez l'actualité de Montpellier!
          br
          br
          a.twitter-timeline(data-dnt='true', href='https://twitter.com/hashtag/montpellier', data-widget-id='938903201243435009') Tweets sur #montpellier
          script.
            !function (d, s, id) { var js, fjs = d.getElementsByTagName(s)[0], p = /^http:/.test(d.location) ? 'http' : 'https'; if (!d.getElementById(id)) { js = d.createElement(s); js.id = id; js.src = p + "://platform.twitter.com/widgets.js"; fjs.parentNode.insertBefore(js, fjs); } }(document, "script", "twitter-wjs");
            h1.
        Tweets
    ul
        each tweet, i in tweets
            - if (tweet.retweeted_status && tweet.retweeted_status.text && tweet.retweeted_status.extended_tweet && tweet.retweeted_status.extended_tweet.full_text)
                li
                    a()= tweet.user.name
                    b(style=" color: #FF33333")   dit :
                    br
                    a(href="mailto:#{123}")= tweet.retweeted_status.extended_tweet.full_text
                    br 
                    br
            - else
                li
                    a()= tweet.user.name
                    b(style=" color: #FF33333")   dit :
                    br
                    a(href="mailto:#{123}")= tweet.text
                    br 
                    br
